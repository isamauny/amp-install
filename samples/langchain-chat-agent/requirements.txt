# Dependency list for the pip path.
#
# KEEP IN SYNC WITH pyproject.toml. Google's Python buildpack docs state that
# when both files are present, "the requirements.txt file takes precedence" —
# so this file is what actually gets installed on the build, and a dependency
# added only to pyproject.toml would silently not be installed.
#
# Lower bounds rather than exact pins: the buildpack resolves the newest
# compatible release at build time. Pin exactly if you need reproducible builds.
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.9

# langchain-anthropic wraps the official Anthropic SDK.
langchain-anthropic>=0.3
langchain-core>=0.3
